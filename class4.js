/* implement queue */
/* implement dequeue */
/* 🔄 তুলনা: Stack vs Queue
বিষয়	Stack	Queue
নিয়ম	LIFO (Last In, First Out)	FIFO (First In, First Out)
উদাহরণ	বইয়ের স্তূপ (উপরের বই আগে উঠবে)	লাইনে দাঁড়ানো মানুষ (আগে আসলে আগে যাবে)
মেথড	push(), pop(), peek()	enqueue(), dequeue(), front()
ব্যবহার	Undo system, Call stack	Message queue, Task scheduling */

/* 
  Queue হলো একটি খুব জনপ্রিয় Data Structure (ডেটা গঠন)।
  এর নিয়ম হলো FIFO → "First In, First Out"
  অর্থাৎ: যে উপাদানটি (item) সবার আগে ঢোকে, সেটিই সবার আগে বের হয়।
  ঠিক যেমন লাইনে দাঁড়ানো মানুষ — আগে যে এসেছে, সে আগে যাবে।
*/

class Queue {
  constructor() {
    // Queue-এর সব ডেটা রাখার জন্য একটি array ব্যবহার করা হচ্ছে।
    this.items = [];
  }

  // enqueue(value) → queue-এর শেষে একটি নতুন মান (value) যোগ করে।
  enqueue(value) {
    // push() মেথড array-এর শেষে নতুন মান যুক্ত করে।
    this.items.push(value);
  }

  // dequeue() → queue-এর শুরু থেকে একটি মান বের করে (remove করে) দেয়।
  dequeue() {
    // যদি queue ফাঁকা থাকে, তাহলে undefined রিটার্ন করবে।
    if (this.isEmpty()) {
      return undefined;
    }

    // shift() array-এর প্রথম উপাদানটি সরিয়ে ফেলে এবং সেটি রিটার্ন করে।
    return this.items.shift();
  }

  // peek() → queue-এর প্রথম মানটা "দেখে" কিন্তু মুছে ফেলে না।
  peek() {
    // যদি queue ফাঁকা থাকে, তাহলে undefined রিটার্ন করবে।
    if (this.isEmpty()) {
      return undefined;
    }

    // queue-এর প্রথম উপাদানটি রিটার্ন করবে (index = 0)
    return this.items[0];
  }

  // isEmpty() → queue ফাঁকা কিনা চেক করে।
  isEmpty() {
    // যদি array-এর length = 0 হয় → true (ফাঁকা)
    // না হলে → false
    return this.items.length === 0;
  }

  // print() → queue-এর সব মান সুন্দরভাবে প্রিন্ট করে।
  print() {
    /* 
      slice() → array-এর একটি কপি তৈরি করে (মূল ডেটা পরিবর্তন না করে)
      reverse() → উল্টে দেয় (শুধু প্রদর্শনের জন্য)
      join(" -> ") → প্রতিটি মানের মাঝে " -> " বসায়
      console.log() → আউটপুট প্রদর্শন করে
    */
    console.log(
      "start -->", // queue-এর শুরু
      this.items.slice().reverse().join(" -> "), // queue-এর উপাদানগুলো
      "--> end" // queue-এর শেষ
    );
  }
}

// ---- Queue ব্যবহার করা ----

// একটি নতুন queue তৈরি করা হলো।
const queue = new Queue();

// enqueue() দিয়ে queue-তে ৩টি মান যোগ করা হলো।
queue.enqueue(10); // প্রথমে 10 ঢোকে
queue.enqueue(20); // পরে 20 ঢোকে
queue.enqueue(30); // শেষে 30 ঢোকে

// এখন queue এর মান হবে [10, 20, 30]
// print() দিয়ে সুন্দরভাবে দেখা যাক।
queue.print();
// Output: start --> 30 -> 20 -> 10 --> end
// (এখানে উল্টে দেখানো হয়েছে, শুধু বুঝতে সুবিধার জন্য। প্রকৃত ক্রম [10, 20, 30])

// dequeue() → প্রথম মানটি (10) বের করে দেবে।
queue.dequeue();

// এখন queue এর মান হবে [20, 30]
queue.print();
// Output: start --> 30 -> 20 --> end

/* 🧠 বিস্তারিত ব্যাখ্যা:
✅ Queue কী?

Queue হলো FIFO (First In, First Out) টাইপ ডেটা স্ট্রাকচার।
মানে:

যে উপাদান আগে ঢোকে, সেটাই আগে বের হয়।

উদাহরণ:
লাইনে দাঁড়ানো → প্রথমে আসা মানুষ আগে সার্ভিস পায়।

✅ Queue-র মেথডগুলো:
মেথড	কাজ
enqueue(value)	Queue-এর শেষে নতুন মান যোগ করে
dequeue()	Queue-এর শুরু থেকে মান বের করে
peek()	Queue-এর প্রথম মানটা দেখে, কিন্তু মুছে না
isEmpty()	Queue ফাঁকা কিনা চেক করে
print()	Queue-এর উপাদানগুলো সুন্দরভাবে প্রিন্ট করে
✅ Array মেথড ব্যাখ্যা:
মেথড	কাজ
push()	array-এর শেষে মান যোগ করে
shift()	array-এর শুরু থেকে মান মুছে ফেলে
slice()	array-এর কপি নেয় (মূল ডেটা না বদলে)
reverse()	array উল্টে দেয়
join(" -> ")	array কে string বানায়, মাঝখানে চিহ্ন দেয়
✅ this এর কাজ:

this মানে বর্তমান Queue অবজেক্ট।

যেমন:

const queue1 = new Queue();
queue1.enqueue(10);


এখানে this মানে queue1।

তাই প্রত্যেক Queue অবজেক্টের নিজস্ব ডেটা (items) থাকে।

✅ Queue কোথায় ব্যবহার হয়?

Task scheduling (কাজের সারি)

Print queue (প্রিন্টারের কাজ)

Message queue (চ্যাট বা সার্ভারে মেসেজ আদান-প্রদান)

Data buffering (ভিডিও/অডিও স্ট্রিমিং) */
